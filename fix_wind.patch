--- index.html
+++ index.html
@@ -628,8 +628,8 @@
             const headS = isHeadwind(wDirS, 'start');
             const headE = isHeadwind(wDirE, 'end');

-            const windBadS = headS && wSpeedS > maxW;
-            const windBadE = headE && wSpeedE > maxW;
+            const windBadS = wSpeedS > maxW;
+            const windBadE = wSpeedE > maxW;
             const fail = tS < minT || tE < minT || appTS < minAppT || appTE < minAppT || rain > maxR || snow > maxS || windBadS || windBadE;

             let verd = "MINAM!", badge = "bg-safe", icon = "🚴";
@@ -647,10 +647,10 @@
                 const failText = badConditions.join(", ");
                 verd = `<div style="text-align: left; line-height: 1.2;">
                     <span style="font-size: 0.9em; font-weight: bold; color: white;">NE</span><br>
-                    <span style="font-size: 0.7em; opacity: 0.9; text-transform: uppercase;">${failText}</span>
+                    <span style="font-size: 0.7em; opacity: 0.9; text-transform: uppercase;">${failText}</span>
                 </div>`;
                 badge = "bg-danger";
                 icon = "🚫";
-            } else if (windBadS || windBadE) {
-                verd = "VĖJAS";
-                badge = "bg-warning";
-                icon = "⚠️";
             }
@@ -677,10 +677,10 @@
             const snowText = isReal ? `<span class="${snow > maxS ? 'text-danger' : ''}">${parseFloat(snow).toFixed(1)} cm</span>` : `<span class="${snow > maxS ? 'text-danger' : ''}">${parseFloat(snow).toFixed(1)} cm</span>`;
             const rainText = isReal ? `<span class="${rain > maxR ? 'text-danger' : ''}">${parseFloat(rain).toFixed(1)} mm</span><br><small class="text-muted">${prob}%</small>` : `<span class="${rain > maxR ? 'text-danger' : ''}">${parseFloat(rain).toFixed(1)} mm</span><br><small class="text-muted">${prob}%</small>`;

-            const windSColor = windBadS ? 'var(--danger)' : (headS ? 'var(--warning)' : 'inherit');
-            const windEColor = windBadE ? 'var(--danger)' : (headE ? 'var(--warning)' : 'inherit');
+            const windSColor = windBadS ? 'var(--danger)' : (headS ? 'var(--warning)' : 'inherit');
+            const windEColor = windBadE ? 'var(--danger)' : (headE ? 'var(--warning)' : 'inherit');

             const windText = `
                 <div class="wind-box">
